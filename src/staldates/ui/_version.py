# Note: this is not the same as the avx library version!
# It is the latter that gets checked for compatibility with the controller.
# not this version string. But it keeps things simpler to keep them in step.
__version__ = "0.96.0"
